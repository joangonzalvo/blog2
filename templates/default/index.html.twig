{% extends 'base.html.twig' %}
{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('assets/css/estilo.css') }}"/>
{% endblock %}
{% block body %}
    
    <div class="container">
        <div class="jumbotron">
            <h1>Blog</h1>
            
        </div>
            {% if app.user %}
        <div class="my-data">
            <h3>User data:</h3>
            User: {{app.user.username}}<br>
            Mail: {{app.user.email}}<br>
            Role: {{app.user.role}}<br>
        </div>
        {% endif %}
    </div>
    
{% endblock %}
